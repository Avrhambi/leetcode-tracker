# AGENTS.md

The agent instructions for this repo live in [`../CLAUDE.md`](../CLAUDE.md) — stack,
commands, key files, architecture invariants, conventions, and delivery rules.

This file is a pointer so tools that look for `AGENTS.md` find the same single source
rather than a second copy that can drift.
